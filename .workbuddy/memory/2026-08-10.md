# 2026-08-10 3DGS Vulkan 空白窗口调试（续）

- **关键澄清（用户问"是否还在算/要等很久"）**：`crash.txt` 显示 `Pass 6/6 Render done` ×4 且持续循环 → 程序**没有卡在运算**，是持续渲染的 viewer，每帧几毫秒完成。GPU 80%+ 与"未响应"= 主线程紧凑渲染循环没泵 Windows 消息（单线程 viewer 常见），不是死锁。空白 = 画了但看不见。
- **已排除 "render 写 A / present 显 B"**：`onRender`(SplatSceneApplication.cpp:201/255/281) 用 `execute()` 返回的同一 `splatOutput` 纹理做 present → 同一纹理。空白只能是"输出纹理每像素透明"（所有高斯被 EWA power/alpha 测试丢弃 → alpha=0 → 灰背景）。
- **已给 `splat_render.comp` 加颜色编码诊断**（可还原）：🔵蓝=该 tile `start>=end`（无高斯，排序/瓦片范围问题）；🔴红=有高斯但全部未过 `power>0` 测试（conic/屏幕坐标问题，最可能是真凶）；🟢绿=有高斯过 power 测试（几何正常，问题在 alpha 量级/颜色）。已用 glslc 重编 `Render/compiled/splat_render.spv`。
- **下一步**：用户重跑 `MonsterEngine.exe 2> crash.txt`（无需 rebuild C++），看窗口主色 → 据颜色定位 conic/坐标或 alpha 真凶。
- 注：之前 DIAG 的 `onScreen=54%/conicOK=38%` 是在**坏排序**下用垃圾索引 OOB 读出的，不可信；修好 scatter 后以真实高斯为准。