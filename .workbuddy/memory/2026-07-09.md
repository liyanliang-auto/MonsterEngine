# 2026-07-09

## 仓库结构梳理（MonsterEngine）
- 用户要求"整理 D:\code\MonsterEngine 代码仓库的文件结构"。
- 产出：`docs/REPO_STRUCTURE.md`（真实目录树 + 模块职责 + 问题清单 + 整理方案）。
- 关键发现：
  1. P0: `3rd-party/imgui` 为空(0文件)，但 `Source/Editor/ImGui/*.cpp` 直接 include imgui.h → CMake 可能缺符号。
  2. P0: 顶层 `Tests/`(TAATests/FXAATests) 未被 CMakeLists 引用，是孤儿测试；测试代码分散在 Tests/、Source/Tests/(16 cpp)、Include/Tests/(9 h) 三处。
  3. P1: README 目录结构段与实际漂移（Scene.h 非目录、漏列 Deferred/PostProcess、imgui 描述不实）；README 引用 `.windsurf/rules/wind-monster-peoject-rule.md` 路径疑似拼错。
  4. P2: 双构建系统(CMake + .vcxproj)并存；glfw 预编译二进制(228文件)入库；`.github/superpowers`+`.windsurf/skills` 约103文件 AI 工具目录污染仓库；根目录散落 Vulkan_PBR_helmet.jpg。
- 整体骨架健康（头/实现按模块镜像，RHI→Platform→Renderer→Engine 分层清晰）。
- 待用户确认是否执行物理整理（阶段A修复构建/B校正文档/C整洁度）。

## 引擎架构文档（MonsterEngine 全量学习）
- 用户要求学习整个引擎代码并把设计/架构/功能整理成文档。
- 产出：`devDocument/monsterEngine引擎架构_20260709.md`（约 11 节，含分层架构、模块详解、渲染主流程、功能状态表、后端矩阵、坑位章节）。
- 方法：用 Explore 子代理 very thorough 扫描全仓抽架构+代码证据；读既有 devDocument 文档（含已过时的 `引擎的架构和设计.md`）；对最关键的"默认路径画不出图"论断亲自 grep+读 `CubeSceneApplication.cpp` 核实（行号锚点）。
- 已核实的关键结论（写入文档第9节）：
  1. 默认开关 `m_bEnableParallelRendering=true`(CubeSceneApplication.cpp:90) + `m_bUseRDG=true`(:516) → 走 `renderWithRDGParallel`(:520)，其 Shadow/PostPass 为 `// TODO` 空壳；`dispatch*`(:2608/:2709/:2758) 录完命令列表后 `RecycleCommandList`(:2700/:2749/:2808) 未提交 GPU → 默认帧近乎空白。能出图的是传统 else 分支(:538)或 `renderWithParallelRenderer`(FParallelSceneRenderer)。
  2. `MeshDrawCommand`/`RenderQueue`/`FVirtualTextureSystem`/`FTextureStreamingManager` 仅在各自 .cpp + `Tests/` + `SceneRenderer.cpp` 出现，未接入主渲染循环。
  3. 三套场景渲染器并存：`Engine::FSceneRenderer`(Source/Engine/SceneRenderer.cpp)、`Renderer::FSceneRenderer`(RendererSceneRenderer.cpp)、`FParallelSceneRenderer`，默认帧循环一个都没真正调用。
  4. 后端矩阵：Vulkan✅(Windows)、OpenGL✅(Windows-only)、D3D12/D3D11/Metal❌仅占位、Android/ES❌计划中。
- 既有 `引擎的架构和设计.md` 等描述旧接口(RHIFactory/TriangleRenderer/IRHIBuffer)，已严重过时，文档以当前代码为准。
